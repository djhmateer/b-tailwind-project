module.exports = {
    plugins: [
        require('tailwindcss'),
        require('autoprefixer'),
        // minification
        // require('cssnano')({
        //     preset: 'default',
        //   }),
    ]
}